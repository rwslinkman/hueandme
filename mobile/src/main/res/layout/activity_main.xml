<!--suppress AndroidElementNotAllowed -->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <!-- Top toolbar -->
    <android.support.v7.widget.Toolbar
        android:id="@+id/mainactivity_toolbar_view"
        style="@style/ToolBarStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/rwslinkman_blue_light"
        android:minHeight="@dimen/abc_action_bar_default_height_material" />


    <!-- DrawerLayout below toolbar (menu + mContainer) -->
    <android.support.v4.widget.DrawerLayout
        android:id="@+id/drawer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/mainactivity_toolbar_view">

        <!-- Container (app content) -->
        <FrameLayout
            android:id="@+id/container"
            android:layout_width="match_parent"
            android:clickable="true"
            android:background="@android:color/white"
            android:layout_height="match_parent" />

        <!-- NavigationDrawer -->
        <android.support.design.widget.NavigationView
            android:id="@+id/navigation"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="@color/rwslinkman_blue_dark"
            app:headerLayout="@layout/drawer_header" />

    </android.support.v4.widget.DrawerLayout>
</RelativeLayout>